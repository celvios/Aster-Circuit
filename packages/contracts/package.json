{
    "name": "@aster-circuit/contracts",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "compile": "hardhat compile",
        "test": "hardhat test",
        "coverage": "hardhat coverage",
        "lint": "solhint 'contracts/**/*.sol'",
        "deploy": "hardhat run scripts/deploy.ts"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "@prb/math": "^4.1.0",
        "dotenv": "^16.0.0",
        "hardhat": "^2.22.0",
        "solhint": "^4.1.1",
        "ts-node": "^10.0.0",
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.0"
    }
}
